/** @type {import('next').NextConfig} */

// const withNextIntl = require('next-intl/plugin')(
//     './i18n.ts'
// );

const nextConfig = {
};

// FIXME: Does not work with next 14
// module.exports = withNextIntl(nextConfig);
module.exports = nextConfig;
